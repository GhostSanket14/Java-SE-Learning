package OOPs2_abstractAndInterfaces;

public abstract class papa { // we can't create object of abstract class. But we can create reference variable of abstract class.

	public void papaData() { // We can make normal function inside abstract class.
		
	}
	
	public abstract void childData(); // But abstract functions can only be declared inside abstract class.
	
//	public void newchildData(); // Can have non-abstract function declaration. they must be defined.
	
}