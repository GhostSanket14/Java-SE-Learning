package OOPs1_AccessModifiers;

public class protectedData {

	protected int d=414; // protected can be accessed by any class that inherit it. 
		// Also by entire package it is created into.

} // This will be accessed by class(using protected) in OOPs1_support package, as well.