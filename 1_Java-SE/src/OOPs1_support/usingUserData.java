package OOPs1_support;

import OOPs_X_All_below_combined.userData;

public class usingUserData {

	public static void main(String[] args) {
		userData u=new userData();
		
		u.Age=22; // This is public so we can use it.
//		u.name="Ghost"; // This is default and hence can't be used.
		
	}
}