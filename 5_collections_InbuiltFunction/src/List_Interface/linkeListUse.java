package List_Interface;

import java.util.*;

public class linkeListUse {

	public static void main(String[] args) {
		
		// LinkedList, can be created to store data of only non-primitive types
		LinkedList<String> names=new LinkedList<>(); // Internally uses Doubly LinkedList		
		
		// All methods from AL work same in LL.
		// Keep in mind LL implement Queue and Dequeue interface so those functions are used.
		// Just remember Dequeue Function(and Queue if need be).
				
		// addFirst() and addLast() give Exception so we use these. offerLast() offerFirst()		
		// getFirst() and getLast()
		// removeFirst() and removeLast()
		// pollFirst() and pollLast()	
	}
}